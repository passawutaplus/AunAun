/** @deprecated Client-side mock catalog removed — data comes from Supabase only. */
export type {
  Category,
  SpecialFilter,
  FeedFilter,
  ProjectStatus,
  HiringStatus,
  Project,
  HiringRequest,
} from "@/data/projectTypes";
export { categories, specialFilters, feedFilters } from "@/data/projectTypes";
