import MarketingInsightPanel from "./MarketingInsightPanel";

/** Ads planner reuses insight runner with ads tab preset. */
export default function MarketingAdsPlanner() {
  return <MarketingInsightPanel />;
}
