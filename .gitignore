.env
.env.*
!.env.example

# Vite cache (Anthem-Code junction workaround)
.vite-cache/

# Cursor debug logs (local session traces)
.cursor/debug-*.log

# Local database / storage dumps (never commit)
backups/
logs/
