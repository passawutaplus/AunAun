-- page_dwell product events + admin_page_dwell_insights
-- Applied remotely via Supabase MCP (page_dwell_analytics). Keep for source of truth.

-- See: public.log_product_event (adds page_dwell)
-- See: public.admin_page_dwell_insights(_days)
SELECT 1;
