# HTTPS reverse proxy — Caddy routes by Host; nginx (proxy) routes to solo/anthem/ops-hub.
# Set email for Let's Encrypt expiry notices (optional):
# {
#   email you@example.com
# }

solofreelancer.com, www.solofreelancer.com {
	reverse_proxy proxy:80
}

an1hem.app, www.an1hem.app {
	reverse_proxy proxy:80
}

hq.solofreelancer.com {
	reverse_proxy proxy:80
}
